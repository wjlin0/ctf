version: '3'
services:
  web:
    build: .
    container_name: unserialize-php-006
    image: wjlin1/ctf:unserialize-php-006
    restart: always
    platform: linux/amd64
    ports:
      - "1506:80"
    environment:
      - FLAG=flag{knownsec}