version: '3'
services:
  web:
    build: .
    container_name: unserialize-php-008
    image: wjlin1/ctf:unserialize-php-008
    restart: always
    platform: linux/amd64
    ports:
      - "1508:80"
    environment:
      - FLAG=flag{knownsec}